## 1. 桌面端收起样式

- [x] 1.1 在 `note-browser.css` 中增加 `.note-sidebar-collapsed` 样式：桌面端收起时目录宽度为 0 或窄条
- [x] 1.2 桌面端收起时，在左侧预留展开按钮区域（或浮动按钮）

## 2. 桌面端切换逻辑

- [x] 2.1 在 `note-browser.js` 中为桌面端增加收起/展开逻辑，切换 `note-sidebar-collapsed` class
- [x] 2.2 桌面端 `note-sidebar-toggle` 点击时：若目录展开则收起，若收起则展开
- [x] 2.3 确保移动端逻辑不变（仍为抽屉开关）

## 3. 切换按钮布局

- [x] 3.1 调整桌面端切换按钮位置：目录展开时在 header 或侧边栏内，收起时在左侧边缘可见
- [x] 3.2 按钮图标/文案随状态变化（如 ≡ / ▶）

## 4. 可选：偏好持久化

- [x] 4.1 将桌面端目录展开/收起状态存入 localStorage
- [x] 4.2 页面加载时读取并恢复状态
