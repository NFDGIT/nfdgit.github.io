## 1. JS 瞄准逻辑改造

- [x] 1.1 将 `onPointerDown` 的触发条件从"必须点中母球区域"改为"Canvas 上任意位置均可触发"
- [x] 1.2 修改击球方向计算：以拖拽起点与母球连线的反方向作为击球方向（而非简单的拖拽反方向）
- [x] 1.3 修改力度计算：使用拖拽起点到当前点的距离，而非从母球出发的距离
- [x] 1.4 改进 `drawAimLine`：方向线改为带箭头的实线，并在母球上叠加半透明方向指示圆环

## 2. 力度进度条

- [x] 2.1 在 `index.html` 的画布下方新增力度进度条 DOM（`<div class="billiards-power-bar"><div id="power-fill"></div></div>`）
- [x] 2.2 在 `billiards.css` 中添加力度进度条样式（绿→黄→红渐变，高度适中，圆角）
- [x] 2.3 在 JS 的 `onPointerMove` 与 `onPointerUp` 中同步更新 `#power-fill` 的 `width`

## 3. 移动端布局改造

- [x] 3.1 在 `index.html` 中新增移动端专用底部控制条（包含状态信息与操作按钮）
- [x] 3.2 在 `billiards.css` 中为 `<= 700px` 添加底部固定控制条样式（`position: fixed; bottom: 0`），游戏区添加对应的 `padding-bottom`
- [x] 3.3 桌面端（`> 700px`）保留现有的侧边面板布局，隐藏底部固定条

## 4. Canvas 尺寸与球尺寸自适应

- [x] 4.1 在 `resizeCanvas` 里检测可用宽度，小于阈值时将逻辑宽高切换为更接近竖屏的紧凑尺寸（如 420×280）
- [x] 4.2 将 `BALL_R` 和 `POCKET_R` 改为随逻辑宽度动态计算的变量，确保球在小桌面上相对更大
- [x] 4.3 将 `createBalls` 中的球初始位置改为基于 `TABLE_W/TABLE_H` 的相对计算，确保自适应后不超出桌面

## 5. 验收

- [x] 5.1 桌面端验证：侧边面板布局完整，任意位置拖拽击球正常，力度条正常显示
- [x] 5.2 模拟移动端（窄屏）验证：底部固定控制条可见，Canvas 充满宽度，球尺寸可操作，触控瞄准与击球正常
