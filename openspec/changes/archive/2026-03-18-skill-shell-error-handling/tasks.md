## 1. 内容补充

- [x] 1.1 在 Step 5 的 Python 脚本之后添加 Bash 脚本示例（check_deps.sh：检查 Node/Python/git 是否安装、检查 package.json/requirements.txt 状态）
- [x] 1.2 将现有的 SKILL.md 引用示例替换为增强版：包含错误处理指令（脚本失败时的降级方案）和依赖检测（先 `which python3`，不存在则提示安装或用替代方案）
