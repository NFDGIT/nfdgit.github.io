## 1. 方向语义改造

- [x] 1.1 在 `games/billiards/billiards.js` 中将击球方向计算从“拖拽同向”改为“拖拽反向”（例如由 `aimCurrent - aimStart` 调整为 `aimStart - aimCurrent` 或等价实现）
- [x] 1.2 同步调整瞄准线渲染方向，确保瞄准线方向与最终出杆方向一致，避免视觉反馈与实际运动不一致

## 2. 2D/3D 一致性与验收

- [x] 2.1 验证 2D 与 3D 模式在相同拖拽轨迹下均满足“拖拽反向出杆”，且两模式结果一致
- [x] 2.2 回归验证力度映射、碰撞、进袋、积分、重置与通关逻辑未受影响，并补充必要说明/注释（如需要）
