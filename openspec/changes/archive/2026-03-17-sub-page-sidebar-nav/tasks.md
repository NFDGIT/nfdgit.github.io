## 1. 子页面布局改造

- [x] 1.1 `tools.html`：用 `agent-layout` 包裹内容区，添加 4 步侧边栏导航，给每个 section 加 `id="step-N"`
- [x] 1.2 `memory.html`：同上，4 步侧边栏
- [x] 1.3 `mcp.html`：同上，4 步侧边栏
- [x] 1.4 `multi-agent.html`：同上，4 步侧边栏
