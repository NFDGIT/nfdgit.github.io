## 1. 3D 视口与相机边界修正

- [x] 1.1 在 `games/billiards/billiards.js` 中重构 3D 相机 framing 计算，确保桌面、边框、袋口完整进入可视范围
- [x] 1.2 修正 3D 渲染尺寸同步逻辑（2D/3D 切换、resize、方向变化），统一更新 renderer 与 camera 参数

## 2. 容器渲染边界与样式对齐

- [x] 2.1 检查并调整 `games/billiards/billiards.css` 中 3D 容器的边界与 overflow 策略，避免桌边被裁切
- [x] 2.2 校正 3D 场景与逻辑桌面边界映射，确保球到达边缘区域时仍保持可见

## 3. 回归验证与稳定性检查

- [x] 3.1 验证 3D 模式下桌面端与移动端（竖屏/横屏）均可完整显示桌边与袋口
- [x] 3.2 验证球在边界附近反弹时持续可见，且 2D/3D 切换、拖拽击球与计分行为无回归
