## 1. 新建保险页专属样式

- [x] 1.1 创建 `insurance/insurance.css`，定义 hero 区、产品卡片区、联系按钮、底部联系条的样式
- [x] 1.2 在样式中使用 `home.css` 的主题变量（`--bg`、`--text`、`--card-bg`、`--card-shadow` 等），确保亮/暗色适配

## 2. 重写 HTML 结构

- [x] 2.1 重写 `insurance/index.html`：添加 hero 区（标题 + 副标题 + 电话/QQ 联系按钮）
- [x] 2.2 将两张产品图片包装为卡片区（产品名称 + 描述 + 图片，使用 `object-fit: cover`）
- [x] 2.3 移除左侧白色固定浮窗，联系方式改为 hero 区按钮
- [x] 2.4 添加移动端底部固定联系条（电话 + QQ）
- [x] 2.5 消除所有内联 `style` 属性，全部使用 CSS 类

## 3. 动画与交互

- [x] 3.1 为 hero 区添加 `.anim-fade-in`，为产品卡片添加 `.anim-slide-up` + `stagger-*`
- [x] 3.2 产品卡片悬停效果（继承 `.home-card` 的 glow + 抬升）

## 4. 响应式与验证

- [x] 4.1 添加移动端媒体查询：卡片单列、按钮全宽、底部联系条仅移动端显示
- [x] 4.2 验证亮/暗色主题切换正常、电话/QQ 链接可点击、动画播放正确
