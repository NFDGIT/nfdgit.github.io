## 1. 依赖与切换入口

- [x] 1.1 在 `games/billiards/index.html` 中通过 CDN 引入 Three.js（如 unpkg 或 jsDelivr），并增加 2D/3D 模式切换控件（按钮或选项）
- [x] 1.2 在 `billiards.js` 或新脚本中增加当前渲染模式状态（2D / 3D），切换时隐藏/显示对应画布或容器，保证同一时刻仅一种渲染激活

## 2. 游戏状态与渲染分离

- [x] 2.1 抽离或暴露「游戏状态」接口（球位置、速度、进袋、积分、瞄准线等），使 2D 与 3D 渲染层均可从同一数据源读取并绘制
- [x] 2.2 确保物理与规则逻辑仅在一处维护，3D 层每帧根据状态更新场景中球与瞄准线的位置/方向，不重复实现碰撞与进袋

## 3. 3D 场景搭建

- [x] 3.1 实现 3D 场景初始化：Scene、相机（俯视或带小倾角）、光源，以及将 Three.js 的 renderer 挂载到页面指定容器
- [x] 3.2 创建 3D 球桌：桌面平面、边框、六个球袋（简单几何体），材质与颜色与主题协调（可读 data-theme 或 CSS 变量）
- [x] 3.3 根据当前游戏状态创建或更新 3D 球体（母球 + 未进袋彩球），球位与 2D 逻辑中的坐标同步；进袋球从 3D 场景中移除或隐藏

## 4. 3D 模式下的交互与循环

- [x] 4.1 在 3D 模式下复用现有拖拽/触控事件：将输入坐标正确映射到 2D 逻辑坐标（或统一在逻辑层处理），保证瞄准方向与力度与 2D 一致
- [x] 4.2 实现 3D 渲染循环（requestAnimationFrame）：每帧根据最新游戏状态更新球与瞄准线在 3D 场景中的位置，并调用 renderer.render

## 5. 切换与降级

- [x] 5.1 实现 2D ↔ 3D 切换：切换时不清空游戏状态，仅切换渲染目标；3D 切换回 2D 时销毁或隐藏 3D 场景并恢复 Canvas 2D 显示
- [x] 5.2 检测 WebGL 或 Three.js 加载失败时回退到 2D 模式，并可选提示用户「3D 不可用」

## 6. 样式与验收

- [x] 6.1 确保 3D 容器与 2D Canvas 在布局与响应式上一致（如同属 `.billiards-canvas-wrap`），移动端与主题切换下 3D 场景正常显示
- [x] 6.2 验收：2D/3D 切换后玩法一致，进袋、积分、重置与通关在两种模式下均正确；3D 下球桌与球立体可见、可瞄准击球
