## 1. 基础设施

- [x] 1.1 在 `base.css` 中新增 `.glass-input` 和 `.glass-textarea` 组件类（半透明背景 + 模糊 + focus 高亮）
- [x] 1.2 创建 `showcase/` 目录

## 2. 演示页面构建

- [x] 2.1 创建 `showcase/showcase.css`：渐变 mesh 背景、区段布局、组件网格、主题对比双栏
- [x] 2.2 创建 `showcase/index.html` 主体结构：hero 区 + 面板区 + 卡片区 + 按钮区 + 输入框区 + overlay 区 + 设计令牌区 + 动画区 + 主题对比区
- [x] 2.3 面板区段：展示 3 种面板变体（不同圆角、嵌套面板、不同模糊度）
- [x] 2.4 卡片区段：展示基础卡片、图标卡片、图片卡片（使用占位色块）
- [x] 2.5 按钮区段：展示默认/主色/药丸/图标/按钮组/禁用态
- [x] 2.6 输入框区段：展示 glass-input 文本框、搜索框、glass-textarea
- [x] 2.7 Overlay 区段：可触发的 glass-overlay 演示（含显示/关闭交互）
- [x] 2.8 设计令牌区段：色板（accent/text/glass-bg）、阴影层级、圆角对比、模糊对比
- [x] 2.9 动画区段：fade-in/slide-up/scale-in/stagger 演示 + 重播按钮
- [x] 2.10 主题对比区段：亮/暗双栏并排展示面板+按钮+卡片

## 3. 交互与集成

- [x] 3.1 创建 `showcase/showcase.js`：overlay 触发/关闭、动画重播逻辑
- [x] 3.2 在 `index.html` 首页卡片网格中添加演示库入口卡片
