## 1. FVM 安装小节

- [x] 1.1 在 `note/IT/flutter_fvm.md` 中新增「FVM 安装」小节（可放在「项目 vs 全局」之前），包含至少 macOS（如 Homebrew / pub global）与 Windows 或通用安装方式及可执行命令，必要时注明 PATH 或官方文档链接

## 2. 常用命令小节

- [x] 2.1 在 `note/IT/flutter_fvm.md` 中新增「常用命令」小节，以表格或列表列出并简要说明：`fvm install`、`fvm use`、`fvm list`、`fvm global`、`fvm flutter` / `fvm dart`、`fvm exec` 等，与现有「推荐用法」互补

## 3. 验证

- [x] 3.1 确认文档结构完整、链接有效，安装与命令内容与现有语气和格式一致，无破坏现有 VSCode 与用法说明
