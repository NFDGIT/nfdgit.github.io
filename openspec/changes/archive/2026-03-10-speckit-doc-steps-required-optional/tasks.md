## 1. 新增「步骤必做与可选」小节

- [x] 1.1 在 `note/AI/speckit.md` 的「常用命令」与「命令效果说明」之间插入小节「步骤必做与可选」，写明：必做为 specify、plan、tasks、implement（最小路径 specify → plan → tasks → implement）；可选为 constitution、clarify（可显式跳过）、analyze；并说明目前未见项目级配置可自动执行或默认跳过某步骤，以 [Spec Kit 官方文档](https://speckit.org/) 为准

## 2. 在常用命令表中标注必做/可选

- [x] 2.1 在 `note/AI/speckit.md` 的常用命令表中为每行增加一列「必做/可选」，或紧接表格后加一句总结：specify、plan、tasks、implement 为必做；constitution、clarify、analyze 为可选

## 3. 验证

- [x] 3.1 确认新增小节与表格/总结与命令效果说明一致，链接有效，表述与 explore 结论一致
